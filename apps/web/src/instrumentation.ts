/**
 * Next.js instrumentation hook (required empty stub in some setups).
 * Home showcase ↔ SQLite sync runs from `scripts/next-dev-with-home-sync.ts` via `pnpm dev`.
 */
export async function register(): Promise<void> {}
